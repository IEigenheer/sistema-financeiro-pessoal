{
  "name": "sistema-financeiro-pessoal",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build --workspace=@sfp/api && npm run build --workspace=@sfp/web",
    "db:generate": "npm run db:generate --workspace=@sfp/api"
  }
}
